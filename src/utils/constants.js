const chartColors = ['#FF6384', '#36A2EB', '#FFCE56', '#DDEE67']; // Цвета для секторов графика
const config = { headers: { 'Content-Type': 'application/x-www-form-urlencoded' } };
const url = `http://qcan.tactics.su/test_ivan.php?load_data`;
const percentages = {};


export {
    chartColors,
    config,
    percentages,
    url
}